node_modules/
published-reports/node_modules
# Secrets — never commit; fill in locally or inject via CI secrets
src/config/.env
/test-results/
/playwright-report/
/blob-report/
/all-blob-reports/
/existing-reports/
/published-reports/
/playwright/.cache/
/Test Files/
/PDFDownloads
test-results.json
logs/app.log
# Gmail OAuth secrets
src/utils/token.json
src/utils/gmail-token.json
# One-time setup script
src/utils/SetupGmail.ts
logs/app.log
# Local dashboard preview (generated by scripts/preview-dashboard.js — never needed in CI)
scripts/preview.html
scripts/.preview-tmp/
# Real captured API network traces — may contain sanitised request/response data
# The seed file (network-traces.json) is checked in; per-worker shards are transient
ai-outputs/traces/worker-*.json